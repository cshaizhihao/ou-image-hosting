# OU-Image Hosting v1.4.1

`v1.4.1` 是一个阻断问题热修版本，重点处理安装后首次进入后台和 CI 检查失败。

## 修复内容

- 创建管理员账号并完成首次引导后，现在会进入后台概览页 `/overview`，不再回到公共上传主页。
- 已完成引导的用户登录后，现在会进入 `/overview`。
- GitHub Actions 的 `ouih` 脚本测试补齐 `sudo` mock，避免非 root runner 误判自动补装 Git 失败。
- E2E 首次安装流程同步新的产品路由：`/` 是公共上传页，后台入口从 `/overview` 开始。

## 验证

- `pnpm check`

